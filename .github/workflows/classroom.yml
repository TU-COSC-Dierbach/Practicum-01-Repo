name: Autograding Tests
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  checks: write
  actions: read
  contents: read
jobs:
  feedback:
    runs-on: ubuntu-latest
    environment: autograding_environment  # Specify the environment where your secret is stored
    if: github.actor != 'github-classroom[bot]'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Scanner_test
      id: scanner_test
      uses: classroom-resources/autograding-command-grader@v1
      with:
        test-name: Scanner_test
        setup-command: |
          javac main.java || exit 1  # Compile the code
        command: ${{ secrets.SCANNER_TEST }}
        timeout: 10
        max-score: 5

    - name: Autograding Reporter
      uses: classroom-resources/autograding-grading-reporter@v1
      env:
        SCANNER_TEST_RESULTS: "${{steps.scanner_test.outputs.result}}"
      with:
        runners: scanner_test






# if grep -q 'java.util.Scanner input = new java.util.Scanner(System.in);' main.java; then
#     exit 0  # Test passed
# else
#     exit 1  # Test failed
# fi
